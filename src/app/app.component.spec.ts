
describe("App-component", () => {


    it('should be true', () => {
        // when this test fails, the 'debug starter text' should be shown above the 'it' above: 
        const a = false;
        expect(a).toBeTruthy();
    })


})